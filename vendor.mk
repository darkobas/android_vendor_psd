# Export rom name
export VENDOR := psd

# PSD version info
PSD_VERSION_MAJOR := 3
PSD_VERSION_MINOR := 0
PSD_VERSION_MAINTENANCE := normal
PSD_TYPE := REMIX
PSD_VERSION_TAG := ALPHA1

# PA version info
ROM_VERSION_MAJOR := 5
ROM_VERSION_MINOR := 0
ROM_VERSION_MAINTENANCE := normal 
ROM_VERSION_TAG := ALPHA1
